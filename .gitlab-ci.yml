image: alpine:latest

pages:
  stage: deploy
  script:
    - mkdir -p .public
    - cp -r src/* .public
    - cp index.html .public/
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - main